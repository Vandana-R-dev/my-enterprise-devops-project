apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enterprise-app.fullname" . }}-test-connection"
  labels:
    {{- include "enterprise-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enterprise-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
